name: action

on:
  push:
    branches:
      - main

jobs:
  lint:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4.1.1
      - name: Install Dependancies
        run: npm i
      - name: Lint
        run: npm run lint

